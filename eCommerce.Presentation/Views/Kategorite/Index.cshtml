@model IEnumerable<EdumindAkademia.Models.Kategorite>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Numri)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Titulli)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Numri)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Titulli)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Numri }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Numri }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Numri })
            </td>
        </tr>
}
    </tbody>
</table>
