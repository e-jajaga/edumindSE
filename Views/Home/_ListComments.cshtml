@model List<Komentet>

@for(int i=0; i<Model.Count; i++)
{
    <div class="row">
        <div class="col-4">@Model[i].Komenti</div>
        <div class="col-4">@Model[i].DataKrijimit</div>
    </div>
}
